{
  "crates/utxo/src/set.rs": [
    [1418, 1, "/// Durability requested for one persisted connect or disconnect.\n"],
    [1421, 2, "    /// Visible now; call [`PersistentUtxoSet::flush`] before acknowledging durability.\n"],
    [1424, 1, "    /// Durable before returning success.\n"],
    [1426, 3, "    /// Durable only if every affected stored row matches its before-image.\n"],
    [1435, 1, "    /// An in-memory mutation failed; the cache must be discarded.\n"],
    [1438, 3, "    /// A storage operation failed. A write or flush may have completed ambiguously.\n    #[error(\"coin storage failed: {0}\")]\n"],
    [1442, 4, "    /// A guarded write applied nothing because a before-image did not match.\n    #[error(\"guarded coin write found a mismatched row\")]\n    ConditionMismatch,\n    /// Stored bytes are malformed or carry a different full transaction ID.\n"],
    [1448, 0, "    /// A failed mutation or flush fenced this cache until the owner reopens it.\n    #[error(\"coin cache requires recovery after a failed mutation or flush\")]\n    RecoveryRequired,\n"],
    [1450, 2, "/// Resident allocations, retained deferred before-images, and stored row count.\n"],
    [1454, 1, "    /// Resident shard-table accounting, not the total live coin set.\n"],
    [1456, 2, "    /// Bytes retained until deferred durability completes.\n"],
    [1459, 1, "    /// Distinct transaction rows in the backing store.\n"],
    [1463, 2, "/// A grouped-record store with a disposable resident cache.\n"],
    [1466, 8, "/// Exclusive mutable access serializes reload, mutation, persistence, and eviction.\n/// The cache is private and starts empty; it cannot contain unpersisted caller state\n/// or invoke a caller's UTXO mutation listener before durability completes.\n///\n/// After a failed mutation or flush, all reads and writes fail closed. The owner\n/// must reconcile storage and construct a fresh cache; this type neither resolves\n/// an ambiguous commit nor implements chainstate recovery. In particular, a CAS\n/// mismatch is a failed precondition, not proof of a concurrent writer.\n///\n/// ```compile_fail\n/// use bitcoin_rs_primitives::OutPoint;\n/// use bitcoin_rs_storage::KvStore;\n/// use bitcoin_rs_utxo::set::PersistentUtxoSet;\n/// fn shared_reload<S: KvStore>(coins: &PersistentUtxoSet<S>, op: &OutPoint) {\n///     let _ = coins.get(op); // A cache refill requires exclusive access.\n/// }\n/// ```\n"],
    [1478, 2, "    resident_order: std::collections::VecDeque<Hash256>,\n    retained_before_images: hashbrown::HashMap<Hash256, Option<Vec<u8>>>,\n    recovery_required: bool,\n"],
    [1483, 2, "    /// Opens an empty cache over `store`; zero budget means no eviction cap.\n    pub fn new(store: S) -> Self {\n"],
    [1486, 1, "            set: UtxoSet::new(),\n"],
    [1489, 2, "            resident_order: std::collections::VecDeque::new(),\n            retained_before_images: hashbrown::HashMap::new(),\n            recovery_required: false,\n"],
    [1494, 3, "    /// Evicts whole records to meet the resident allocation budget.\n    /// Deferred before-images pin their rows until `flush`; the caller must bound\n    /// its deferred window separately. Zero removes the eviction cap.\n"],
    [1499, 0, "        self.evict_over_budget();\n"],
    [1501, 12, "    /// Connects one block, persisting only changed transaction rows.\n"],
    [1514, 1, "        &mut self,\n"],
    [1519, 14, "        tracing::trace!(%block_hash, \"persist coin changes\");\n        self.apply(&changes.adds, &changes.removes, mode)\n"],
    [1535, 2, "    /// Applies the exact inverse through the same persistence path.\n"],
    [1538, 1, "        &mut self,\n"],
    [1542, 3, "        self.apply(&undo.restores, &undo.removes, mode)\n    }\n\n    fn apply(\n        &mut self,\n        adds: &[UtxoAdd],\n        removes: &[OutPoint],\n        mode: CoinDurability,\n    ) -> Result<(), PersistentUtxoError> {\n        self.ensure_healthy()?;\n        let affected: hashbrown::HashSet<Hash256> = adds.iter()\n            .map(|add| add.outpoint.txid.into())\n            .chain(removes.iter().map(|op| op.txid.into()))\n            .collect();\n        for txid in &affected {\n            if !self.set.has_live_outputs_for_txid(txid) {\n                self.reload_record(txid)?;\n            }\n        }\n"],
    [1546, 4, "        // A partial in-memory mutation or ambiguous write must never become a\n        // readable, apparently committed cache. Reopen is explicit, not a retry.\n        self.recovery_required = true;\n        self.set.commit_adds_and_removes(adds, removes)?;\n        let after = self.snapshot_images(&affected);\n"],
    [1551, 1, "        self.recovery_required = false;\n        for (txid, image) in &after {\n            self.resident_order.retain(|seen| seen != txid);\n            if image.is_some() {\n                self.resident_order.push_back(*txid);\n            } else {\n                self.remove_resident(txid);\n            }\n        }\n"],
    [1556, 7, "    /// Completes deferred durability, releases pins, and enforces the cache cap.\n    pub fn flush(&mut self) -> Result<(), PersistentUtxoError> {\n        self.ensure_healthy()?;\n        self.recovery_required = true;\n        self.store.flush()?;\n        self.retained_before_images.clear();\n        self.recovery_required = false;\n        self.evict_over_budget();\n"],
    [1566, 33, "    /// Returns an owned output, absence, or a typed storage/corruption error.\n    /// A missing output in a resident transaction does not trigger another read.\n    pub fn get(&mut self, outpoint: &OutPoint) -> Result<Option<TxOut>, PersistentUtxoError> {\n        self.ensure_healthy()?;\n        let txid = outpoint.txid.into();\n        if !self.set.has_live_outputs_for_txid(&txid) {\n            self.reload_record(&txid)?;\n"],
    [1600, 0, "        let output = self.set.get(outpoint);\n        self.evict_over_budget();\n        Ok(output)\n"],
    [1602, 12, "    /// Streams the stored row count. Iterator failures are not empty storage.\n    pub fn ledger(&self) -> Result<CoinLedger, PersistentUtxoError> {\n        self.ensure_healthy()?;\n        let mut stored_rows = 0;\n        for row in self.store.iter_prefix(bitcoin_rs_storage::ColumnFamily::CoinRecords, b\"\")? {\n            row?;\n            stored_rows += 1;\n"],
    [1615, 1, "        Ok(CoinLedger {\n            resident: self.set.memory_report(),\n            retained_before_image_bytes: self.retained_before_images.values()\n                .map(|image| image.as_ref().map_or(0, Vec::len)).sum(),\n            stored_rows,\n        })\n"],
    [1618, 9, "    fn ensure_healthy(&self) -> Result<(), PersistentUtxoError> {\n        if self.recovery_required {\n            Err(PersistentUtxoError::RecoveryRequired)\n        } else {\n            Ok(())\n"],
    [1628, 1, ""],
    [1633, 1, "        affected: &hashbrown::HashSet<Hash256>,\n"],
    [1635, 5, "        affected.iter().map(|txid| {\n            let key = UtxoKey::from_txid(&Txid::from(*txid));\n            (*txid, self.set.shards[usize::from(key.shard())].record_bytes(key, *txid))\n        }).collect()\n"],
    [1642, 28, ""],
    [1671, 1, "        &mut self,\n"],
    [1676, 0, "        use bitcoin_rs_storage::{ColumnFamily::CoinRecords, WriteCondition};\n"],
    [1677, 13, "        for (txid, image) in after {\n            if before.get(txid) == Some(image) {\n                continue;\n"],
    [1691, 2, "            match image {\n                Some(bytes) => batch.put(CoinRecords, txid.as_byte_array(), bytes),\n                None => batch.delete(CoinRecords, txid.as_byte_array()),\n"],
    [1696, 35, "            CoinDurability::Durable => self.store.write_durable(batch)?,\n            CoinDurability::CasGuarded => {\n                let conditions: Vec<_> = before.iter().map(|(txid, image)| match image {\n                    Some(bytes) => WriteCondition::Equals {\n                        cf: CoinRecords, key: txid.as_byte_array(), expected: bytes,\n                    },\n                    None => WriteCondition::Absent {\n                        cf: CoinRecords, key: txid.as_byte_array(),\n                    },\n                }).collect();\n                if !self.store.write_durable_if(&conditions, batch)? {\n                    return Err(PersistentUtxoError::ConditionMismatch);\n"],
    [1733, 11, "            CoinDurability::Deferred => {\n                self.store.write_deferred(batch)?;\n                for (txid, image) in before {\n                    if after.get(txid) != Some(image) {\n                        self.retained_before_images.insert(*txid, image.clone());\n"],
    [1751, 10, "    fn remove_resident(&self, txid: &Hash256) {\n        let key = UtxoKey::from_txid(&Txid::from(*txid));\n        self.set.shards[usize::from(key.shard())].remove_resident_record(key, *txid);\n"],
    [1763, 3, "    fn evict_over_budget(&mut self) {\n"],
    [1769, 13, "        // Visit each candidate at most once; keep pinned rows in the queue so\n        // flush can evict them later rather than losing their residency entries.\n        for _ in 0..self.resident_order.len() {\n            if self.set.memory_report().accounted_bytes() <= self.resident_budget_bytes {\n                break;\n            }\n            let Some(txid) = self.resident_order.pop_front() else { break };\n            if self.retained_before_images.contains_key(&txid) {\n                self.resident_order.push_back(txid);\n            } else {\n                self.remove_resident(&txid);\n            }\n"],
    [1785, 17, "    fn reload_record(&mut self, txid: &Hash256) -> Result<(), PersistentUtxoError> {\n        let Some(stored) = self.store.get(\n            bitcoin_rs_storage::ColumnFamily::CoinRecords, txid.as_byte_array(),\n        )? else {\n            return Ok(());\n"],
    [1803, 0, "        let record = crate::record::UtxoRecord::from_stored_bytes(&stored)\n            .map_err(|_| PersistentUtxoError::CorruptStoredRecord((*txid).into()))?;\n        if record.txid() != *txid || record.is_empty() {\n            return Err(PersistentUtxoError::CorruptStoredRecord((*txid).into()));\n        }\n"],
    [1805, 2, "        self.resident_order.retain(|seen| seen != txid);\n        self.resident_order.push_back(*txid);\n        Ok(())\n"],
    [1809, 5, ""]
  ]
}
